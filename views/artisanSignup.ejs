<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- BOOTSTRAP STYLESHEET -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <script defer src="../public/js/script.js" type="text/javascript"></script>


    <link rel="stylesheet" href="../public/css/style.css">
    <title>ArtiConnect | Artisan Sign Up</title>
</head>

<body>
    <%- include('../public/assets/navBar.ejs') %>

    <main>
        <div class="container">
            <div class="row d-flex justify-content-center">
                <div class="col-lg-6">
                    <h2>Artisan Registration Form</h2>

                    <% if(error_msg) { %>
                        <p style="color:red"><%= error_msg %></p>
                    <% } %>

                    <% if(message) { %>
                        <p style="color:green"><%= message %></p>
                    <% } %>

                    <form action="" method="POST" enctype="application/x-www-form-urlencoded">
                        <div class="form-group my-3">
                            <input type="text" name="contactName" class="form-control" placeholder="Contact Name">
                        </div>

                        <div class="form-group my-3">
                            <input type="text" name="contactPhone" class="form-control" placeholder="Contact Phone">
                        </div>

                        <div class="form-group my-3">
                            <input type="text" name="contactEmail" class="form-control" placeholder="Contact Email">
                        </div>

                        <div class="form-group my-3">
                            <input type="text" name="businessName" class="form-control" placeholder="Business Name">
                        </div>

                        <div class="form-group my-3">
                            <label for="cacStatus">Are you registered with CAC?</label>
                            Yes <input type="radio" name="cacStatus" class="" value="Yes">
                            No  <input type="radio" name="cacStatus" class="" value="No">
                        </div>

                        <div class="form-group my-3 d-flex">
                            <!-- JOB TITLE -->
                            <select name="jobTitle" class="form-control mx-1">
                                <option value="">Your Job Title</option>
                                <!-- ITERATE THROUGH ALL THE JOB TITLES HERE -->
                                <% let jobTitle = ['Graphic designer', 'Web developer', 'UI/UX designer', 'Writer', 'Electrician', 'Make up artist', 'Vulcanizer', 'Tailor', 'Teacher'] %>
                                <!-- ITERATE THROUGH STAFF STRENGTHS HERE -->
                                <optgroup>
                                    <% jobTitle.forEach(title => { %>
                                        <option value="<%= title %>"><%= title %></option>
                                    <% }) %>
                                </optgroup>
                            </select>

                            <!-- STAFF STRENGTH -->
                            <select name="staffStrength" class="form-control mx-1">
                                <option value="">Your Staff Strength</option>
                                <% let staffStrength = ['1-25', '26-50', '51-75', '76-100', '>100'] %>
                                <!-- ITERATE THROUGH STAFF STRENGTHS HERE -->
                                <optgroup>
                                    <option value="freelancer">Freelancer</option>
                                    <% staffStrength.forEach(strength => { %>
                                        <option value="<%=strength%>"><%= strength %> employees</option>
                                    <% }) %>
                                </optgroup>
                            </select>
                        </div>

                        <div class="form-group my-3">
                            <input type="text" name="contactAddress" class="form-control" placeholder="Contact Address">
                        </div>

                        <div class="form-group my-3">
                            <select name="state" class="form-control">
                                <option value="">State</option>
                                <% let states = ['Lagos', 'Abuja', 'Osun', 'Oyo', 'Ogun', 'Sokoto', 'Calabar', 'Abia', 'Imo'] %>

                                <!-- LOOP THROUGH ARRAY OF LOCATIONS HERE -->

                                <optgroup>
                                    <% states.forEach(state => { %>
                                        <option value="<%= state %>"><%= state %></option>
                                    <% }) %>
                                </optgroup>
                            </select>
                        </div>

                        <div class="form-group my-3">
                            <input type="text" name="website" class="form-control" placeholder="Website URL">
                        </div>

                        <div class="form-group my-3">
                            <input type="text" name="username" class="form-control" placeholder="Preferred Username">
                        </div>

                        <div class="form-group my-3">
                            <input type="password" name="password" class="form-control" placeholder="Password">
                        </div>

                        <div class="form-group my-3">
                            <input type="url" name="profilePicture" class="form-control" placeholder="(Https://) Profile Picture">
                        </div>


                        <div class="d-flex justify-content-between align-items-baseline">
                            <input type="submit" class="btn btn-dark biz-pink-bg " value="Register">
                            <p class="mt-2">Already have an account? <a href="/artisan/login"
                                    class="biz-pink d-inline">Log in</a></p>
                        </div>


                    </form>

                </div>
            </div>
        </div> 
    </main>

    <div class="artisan-footer">
        <%- include('../public/assets/footer.ejs') %>
    </div>
         
</body>

</html>